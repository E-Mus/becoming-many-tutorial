{
  "name": "weiss1",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Weiß1 — Partikel-Tutorial für Becoming Many",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "check": "esbuild scripts/check-shapes.ts --bundle --platform=node --format=esm --outfile=node_modules/.cache-check.mjs --log-level=warning && node node_modules/.cache-check.mjs",
    "verify": "npm run typecheck && npm run check && npm run build",
    "sync": "node scripts/sync-to-nas.mjs"
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@types/node": "^26.2.0",
    "@types/three": "^0.185.4",
    "@webgpu/types": "^0.1.60",
    "esbuild": "^0.25.0",
    "typescript": "^5.9.0",
    "vite": "^7.0.0"
  }
}
